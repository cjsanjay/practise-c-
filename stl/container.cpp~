#include<iostream>
#include<vector>

using namespace std;


void checkVector(){
	vector<int> v;
	vector<int> v1;
	v.push_back(1);
	v.push_back(2);
	v.swap(v1);
	for(int i=0;i<v.size();i++){
		cout<<v[i];
	}
}

int main(int argc, char *argv[]){
	checkVector();
}
